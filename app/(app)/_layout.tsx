
export default function AppLayout() {
  // This layout simply redirects to the (tabs) group.
  // This is useful if you want to add other top-level
  // navigation (like a modal) to the (app) group later.
}